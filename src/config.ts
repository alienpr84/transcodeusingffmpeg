import { resolve } from 'path';

export default {
  video: {
    inputAbsolutePath: 'videos/input',
    outputAbsolutePath: 'videos/output',
    inputRelativePath: './videos/input',
    outputRelativePath: './videos/output',
  }
}